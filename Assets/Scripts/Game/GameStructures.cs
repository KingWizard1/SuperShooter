using System;
using UnityEngine;

public enum NetworkMode
{
    SinglePlayer = 0,
    MultiplayerHost = 1,
    MultiplayerClient = 2
}

public enum GameMode
{
    Slayer = 0,
    TeamDeathmatch = 1,
    CaptureTheFlag = 2
}
